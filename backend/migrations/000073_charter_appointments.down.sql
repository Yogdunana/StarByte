-- Keep office assignments and policy history: deleting them would invalidate approvals.
-- Compatibility rollback intentionally retains additive tables/columns and corrected admin identity.
SELECT 1;
